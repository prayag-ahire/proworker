import Router from "express"
import { Request,Response } from "express"

const profile = Router();


profile.get("/profile/me",(req:Request,res:Response)=>{

})

profile.post("/profile/me",(req:Request,res:Response)=>{

})

profile.post("/profile/me/image",(req:Request,res:Response)=>{

})

profile.post("/profile/me/video",(req:Request,res:Response)=>{
    
})
