import { response, Router } from "express"
import { Request,Response } from "express"

const review = Router();

review.post("/Bookingid/review",(req:Request,res:Response)=>{

})

review.delete("/bookingid/review/:id",(req:Request,res:Response)=>{

})
